# Web UI Automation Framework

Python + Playwright + Pytest with POM pattern

## Setup

```bash
pip install -r requirements.txt
playwright install
```

## Run Tests

```bash
pytest
```

## Structure

- `pages/` - Page Object classes
- `tests/` - Test cases
- `config/` - Configuration
- `conftest.py` - Pytest fixtures
